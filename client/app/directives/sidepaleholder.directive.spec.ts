import { SidepaleholderDirective } from './sidepaleholder.directive';

describe('SidepaleholderDirective', () => {
  it('should create an instance', () => {
    const directive = new SidepaleholderDirective();
    expect(directive).toBeTruthy();
  });
});
